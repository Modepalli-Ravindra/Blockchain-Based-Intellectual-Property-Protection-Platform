# Frontend Environment Variables
# Vite automatically loads environment variables starting with VITE_
# If you need to expose variables to the frontend, prefix them with VITE_

# Example: Custom API base URL (currently hardcoded in blockchainService.ts)
# VITE_API_BASE_URL=http://localhost:5000/api

# Example: Enable debug mode
# VITE_DEBUG=true